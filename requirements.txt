# Dépendances Python — Brussels minimal vector tile
#
# Installation :
#   pip install -r requirements.txt
#
# CI (GitHub Actions) :
#   pip install -r requirements.txt --break-system-packages

# Scripts de pipeline
pyyaml>=6.0       # build_map.py, retro_style.py, retro.yml
shapely>=2.0      # merge_buildings.py

# Tests Python
pytest>=7.0       # tests/test_roundtrip.py
